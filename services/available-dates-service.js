
/**
 * 
 * @param {number} duration - duración del trámite (en minutos, entero) que se busca agendar
 * @param {string} date - datetime a partir del cual se busca agendar el trámite. Se tomará la siguiente hora para 
 * 
 * @return {Object[]}}
 */
const availableDates = (duration, date) => {

}

module.exports = {
    availableDates
}