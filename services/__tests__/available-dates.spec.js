const { availableDates }= require("../available-dates-service");

describe("Prueba de la funcion _availableDates_", () => {
    test("Test", async () => {
        expect(true).toBe(false);
      });
});